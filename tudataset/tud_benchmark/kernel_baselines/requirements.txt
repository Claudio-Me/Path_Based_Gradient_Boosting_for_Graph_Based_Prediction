# Build requirements for the C++ graph-kernel extension.
#
# System packages (install with your OS package manager, not pip):
#   g++      C++11-capable compiler
#   eigen3   header-only linear algebra library
#
# Python build dependency:
pybind11>=2.10.0
